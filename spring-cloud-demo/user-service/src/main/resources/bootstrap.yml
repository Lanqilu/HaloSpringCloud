spring:
  application:
    name: user-service # 服务名称
  profiles:
    active: dev # 开发环境，这里是 dev
  cloud:
    nacos:
      server-addr: halo:8848 # Nacos地址
      config:
        file-extension: yaml # 文件后缀名  cloud:
      discovery:
        cluster-name: HZ
        namespace: 45e30304-1b64-4c21-8c83-22309949af10 # 命名空间，填ID